# Battle Planner

Business planner built on top of a forge-compatible battle environment adapter.

Keep scenario logic, prompts, workflows, policies, metrics, and experiments here
instead of placing them in `forge.lib` or `forge.manager`.
